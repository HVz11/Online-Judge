type User {
  id: ID!
  username: String!
  email: String!
}

type Problem {
  id: ID!
  title: String!
  description: String!
  difficulty: String!
}

type Submission {
  id: ID!
  user: User!
  problem: Problem!
  code: String!
  status: String!
}
